# Environment configuration for User & Profile Service
# Copy this to .env and update values for your environment

# ============================================================
# Local Development Configuration
# ============================================================
DB_USER=ms1_user
DB_PASSWORD=password123
DB_HOST=localhost
DB_PORT=3306
DB_NAME=ms1_db

# Use SQLite for development (set to "true" to enable)
USE_SQLITE=false

# API Configuration
FASTAPIPORT=8000

# Logging
SQL_ECHO=false

# ============================================================
# Cloud Run Deployment Configuration
# ============================================================
# When deploying to Google Cloud Run with Cloud SQL:
#
# 1. Create Cloud SQL instance in your GCP project
# 2. Set these environment variables in Cloud Run settings:
#
#    DB_HOST=<your-cloud-sql-instance-ip-or-socket>
#    DB_USER=ms1_user
#    DB_PASSWORD=<your-secure-password>
#    DB_PORT=3306
#    DB_NAME=ms1_db
#
# 3. For Cloud SQL proxy connection (recommended):
#    DB_HOST=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
#
# Example for Cloud Run with Cloud SQL Proxy Socket:
# ============================================================
# DB_HOST=/cloudsql/my-project:us-central1:ms1-db
# DB_USER=ms1_user
# DB_PASSWORD=SecurePassword123!
# DB_PORT=3306
# DB_NAME=ms1_db
# FASTAPIPORT=8080
# USE_SQLITE=false
